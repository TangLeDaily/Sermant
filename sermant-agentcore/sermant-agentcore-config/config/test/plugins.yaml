# 默认编译场景使用，不含示例工程
plugins:
  - service-router
  - service-registry
  - dynamic-config
  - monitor
  - springboot-registry
  - mq-consume-deny
  - service-removal
  - service-visibility
  - tag-transmission
profiles:
  cse:
    - flowcontrol
    - service-router
    - service-registry
    - dynamic-config
  apm:
    - flowcontrol
    - service-router
profile: cse,apm
